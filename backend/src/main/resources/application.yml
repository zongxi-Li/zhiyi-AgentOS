# 服务器配置（支持环境变量）
server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: kinlin-ai-backend

  profiles:
    active: dev

  # 数据库配置（支持环境变量）
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/kinlin_ai}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:ROOT}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # Redis配置（支持环境变量）
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
      password: ${SPRING_REDIS_PASSWORD:}
      database: ${SPRING_REDIS_DATABASE:0}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

# AI服务配置（支持环境变量）
ai:
  service:
    url: ${AI_SERVICE_URL:http://localhost:8000}
    timeout: ${AI_SERVICE_TIMEOUT:30000}

# 应用配置
app:
  upload:
    dir: uploads
    max-size: 10485760  # 10MB
  jwt:
    # JWT密钥 - 至少64字符（512位）用于HS512算法
    # 支持环境变量: APP_JWT_SECRET
    secret: ${APP_JWT_SECRET:kinlin-ai-secret-key-change-in-production-this-is-a-very-long-secret-key-for-hs512-algorithm-must-be-at-least-64-characters-long}
    expiration: ${APP_JWT_EXPIRATION:86400000}  # 24小时（毫秒）

# CORS跨域配置
cors:
  allowed-origins:
    - http://localhost:3000
    - http://localhost:5173
    - http://localhost:5174
    - http://localhost:8080
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
    - PATCH
  allowed-headers: "*"
  allow-credentials: true

# 日志配置
logging:
  level:
    com.kinlin.ai: INFO
    org.springframework.web: INFO
    org.hibernate: WARN

# Agent configuration
agent:
  enabled: ${AGENT_ENABLED:true}
  timeout-ms: ${AGENT_TIMEOUT_MS:30000}
  trace-enabled: ${AGENT_TRACE_ENABLED:false}
  python:
    lawyer-chat-url: ${AGENT_PYTHON_LAWYER_CHAT_URL:http://localhost:8000/ai/agent/lawyer/chat}
    teacher-chat-url: ${AGENT_PYTHON_TEACHER_CHAT_URL:http://localhost:8000/ai/agent/teacher/chat}
    programmer-chat-url: ${AGENT_PYTHON_PROGRAMMER_CHAT_URL:http://localhost:8000/ai/agent/programmer/chat}
    writer-chat-url: ${AGENT_PYTHON_WRITER_CHAT_URL:http://localhost:8000/ai/agent/writer/chat}
  federated:
    enabled: ${AGENT_FEDERATED_ENABLED:false}
    trace: ${AGENT_FEDERATED_TRACE:true}
