fastapi>=0.116,<1
PyYAML>=6.0,<7.0  # Workflow pack manifest parsing
uvicorn[standard]>=0.35,<1
pydantic>=2.7.4,<3.0
pydantic-settings>=2.10,<3
jsonschema>=4.26,<5
python-multipart>=0.0.20,<1
httpx>=0.28,<1
python-dotenv>=1.1,<2
requests>=2.32,<3
openai>=1.90,<3  # 通义千问 OpenAI 兼容模式支持
dashscope>=1.23.1  # 阿里云DashScope SDK（语音识别、语音合成、图像生成）

# 其他依赖
numpy>=2.3,<3
pydub>=0.25,<1
psutil>=7,<8  # 系统性能监控

# 可选依赖（用于增强功能）
# chromadb==0.4.15  # 向量数据库
# sentence-transformers==2.2.2  # 文本向量化
# jieba==0.42.1  # 中文分词

# 测试依赖
pytest>=8.4,<9
pytest-asyncio>=1.1,<2
pytest-cov>=7,<8

# 文档处理增强（推荐安装）
PyPDF2>=3.0,<4  # PDF解析（基础）
pdfplumber>=0.11,<1  # PDF解析（增强）
python-docx>=1.2,<2  # Word文档解析
openpyxl>=3.1,<4  # Excel解析
pandas>=2.3,<3  # 数据处理
beautifulsoup4>=4.13,<5  # HTML解析
lxml>=6,<7  # XML解析（beautifulsoup4依赖）

# 高级文档处理工具（可选）
# easydoc==*  # 文档处理工具（如需使用，请安装）
# mineru==*  # PDF解析工具（高级PDF解析，如需使用，请安装）

# RAG工具集成（可选）
# ragflow==*  # RagFlow RAG工具（如需使用，请安装）
# qanything==*  # QAnything RAG工具（如需使用，请安装）
# fastgpt-client==*  # FastGPT客户端（如需使用，请安装）

# 向量数据库和Embedding（推荐）
chromadb>=1.0,<2  # 向量数据库（稳定版本）
sentence-transformers>=5.1,<6.0  # 与新版 transformers/huggingface_hub 兼容
huggingface_hub>=1.0,<2.0

