fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
httpx==0.25.2
python-dotenv==1.0.0
requests==2.31.0
openai==1.12.0  # 通义千问 OpenAI 兼容模式支持
dashscope>=1.23.1  # 阿里云DashScope SDK（语音识别、语音合成、图像生成）

# 其他依赖
numpy==1.24.3
pydub==0.25.1
psutil==5.9.6  # 系统性能监控

# 可选依赖（用于增强功能）
# chromadb==0.4.15  # 向量数据库
# sentence-transformers==2.2.2  # 文本向量化
# jieba==0.42.1  # 中文分词

# 测试依赖
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# 文档处理增强（推荐安装）
PyPDF2==3.0.1  # PDF解析（基础）
pdfplumber==0.10.3  # PDF解析（增强）
python-docx==1.1.0  # Word文档解析
openpyxl==3.1.2  # Excel解析
pandas==2.1.3  # 数据处理
beautifulsoup4==4.12.2  # HTML解析
lxml==4.9.3  # XML解析（beautifulsoup4依赖）

# 高级文档处理工具（可选）
# easydoc==*  # 文档处理工具（如需使用，请安装）
# mineru==*  # PDF解析工具（高级PDF解析，如需使用，请安装）

# RAG工具集成（可选）
# ragflow==*  # RagFlow RAG工具（如需使用，请安装）
# qanything==*  # QAnything RAG工具（如需使用，请安装）
# fastgpt-client==*  # FastGPT客户端（如需使用，请安装）

# 向量数据库和Embedding（推荐）
chromadb==0.4.15  # 向量数据库（稳定版本）
sentence-transformers>=5.0,<6.0  # 与新版 transformers/huggingface_hub 兼容
huggingface_hub>=0.34,<1.0

