# AgentOS 架构设计

## 分层原则

AgentOS 的核心定位是运行时内核，不承载任何具体行业 Agent 的硬编码实现。

- `agentOS/`：核心运行时。分三层：**顶层 Interface/Abstract**（agents、skills、adapters、governance、communication、memory、recovery 等抽象定义）、**Core Runtime 实现**（`core/` 子包：ACG 引擎、执行适配器、认知规划器、低熵通信实现、治理实现、工作流编排）、**基础设施**（`infrastructure/` 和 `stores/` 的具体存储/适配器实现）。
- `agent/`：应用服务层。负责 FastAPI 入口、业务编排、兼容旧接口、加载启用哪些领域 Pack。LLM 网关和 RAG 检索的实现也在此层。
- `agent/packs/`：领域能力包。法律、教育、编程、写作等具体 Agent、Skill、Workflow、Prompt 和领域数据。
- `agentOS/knowledge/`：知识库服务抽象（向量存储接口、检索器接口）。
- `agentOS/llm/`：LLM 网关服务抽象（Provider Interface、Router Interface）。

> **注**：L4 文档中原始设计的 `knowledge/`、`llm/`、`shared/` 作为独立服务目录，实际实现中：
> - `llm/` 和 `knowledge/` 的 **Interface 层**在 `agentOS/llm/` 和 `agentOS/knowledge/` 下；
> - 具体 RAG 实现（文档解析、分块、检索）在 `agent/app/rag/`；
> - 具体 LLM Provider 实现（DeepSeek、Qwen）在 `agent/app/llm/`；
> - `shared/` 未单独建立，共享类型和工具分散在各模块中。

核心规则：

1. `agentOS/src/agentos/core` 不允许直接 import 任何具体领域 Pack。
2. `agentOS/src/agentos/agents` 只保留通用 Agent Interface 和 Registry。
3. 具体领域 Agent 通过 Pack Manifest 和 Pack Registry 注入运行时。
4. 新增行业能力时，只新增 `agent/packs/<pack_id>/`，不修改 AgentOS Core。
5. Core 面向 Interface 编程，应用层负责选择 Adapter 和 Pack。

---

## 目录结构

```text
agentOS/                              # AgentOS 运行时内核
│   ├── src/
│   │   ├── agentos/                  # AgentOS 核心包
│   │   │   ├── __init__.py
│   │   │   │
│   │   │   ├── core/                 # 核心运行时（顶层 runtime + 6 个子系统）
│   │   │   │   ├── __init__.py
│   │   │   │   ├── runtime.py        # WorkflowRuntime 主类（~668 行，最大文件）
│   │   │   │   ├── acg/              # ACG 动态群体智能引擎
│   │   │   │   │   ├── __init__.py   # 扁平重导出所有公共 API
│   │   │   │   │   ├── enums.py      # NodeType / EdgeType / ControlType / ComplexityLevel
│   │   │   │   │   ├── nodes.py      # StepNode / AgentNode / SkillNode / MemoryNode / EvidenceNode / ControlNode
│   │   │   │   │   ├── edges.py      # ACGEdge（统一边模型）
│   │   │   │   │   ├── blueprint.py  # ACGBlueprint（主容器，含图遍历方法）
│   │   │   │   │   ├── graph_ops.py  # detect_cycle / topological_order / validate_blueprint / ready_steps
│   │   │   │   │   └── promote.py    # promote_workflow_to_acg（线性→ACG 升格）
│   │   │   │   ├── execution/        # 执行适配器层
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── adapters.py   # ExecutionAdapter 协议 / NativeWorkflowAdapter / ACGWorkflowAdapter
│   │   │   │   │   ├── acg_executor.py  # ACGExecutor（基于就绪集的并行步骤调度）
│   │   │   │   │   ├── fault_injection.py  # FaultInjector（可控故障注入验证自愈）
│   │   │   │   │   └── step_wrapper.py    # StepExecutionTimer / summarize
│   │   │   │   ├── communication/    # 低熵通信实现
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── contract.py   # ContextPack / estimate_tokens
│   │   │   │   │   ├── assembler.py  # ContextAssembler（按字段精准装配下游上下文）
│   │   │   │   │   └── audit.py      # ProvenanceLedger / DataProductionEvent / DataConsumptionEvent
│   │   │   │   ├── governance/       # 治理实现
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── trace.py      # TraceStore（审计事件日志）
│   │   │   │   │   ├── checkpoint.py # CheckpointStore（运行时状态快照）
│   │   │   │   │   ├── review.py     # ReviewManager（人机协同审批）
│   │   │   │   │   └── evaluation.py # WorkflowEvaluator（聚合治理指标）
│   │   │   │   ├── models/           # Pydantic 共享领域类型
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   └── types.py      # AgentTask / WorkflowRun / TraceEvent / Checkpoint / 16 种枚举
│   │   │   │   ├── planning/         # 认知规划引擎（"静态优选，动态补位"）
│   │   │   │   │   ├── __init__.py
│   │   │   │   │   ├── engine.py     # PlanningEngine（编排 IntentParser + TemplateMatcher + CognitiveRouter + ACGBuilder）
│   │   │   │   │   ├── intent_parser.py  # IntentParser（LLM + 关键词 fallback）
│   │   │   │   │   ├── profile.py        # TaskSemanticProfile
│   │   │   │   │   ├── template_matcher.py  # TemplateMatcher（基于 n-gram 的 Dice 相似度匹配）
│   │   │   │   │   ├── cognitive_router.py  # CognitiveRouter（多维效用评分 + 临时角色生成）
│   │   │   │   │   └── acg_builder.py     # ACGBuilder（语义画像→ACGBlueprint 物化）
│   │   │   │   └── workflow/         # 工作流编排
│   │   │   │       ├── __init__.py
│   │   │   │       ├── orchestrator.py  # Orchestrator（步骤分派 & 最终输出组装）
│   │   │   │       ├── registry.py      # WorkflowRegistry（YAML/JSON 加载，别名支持）
│   │   │   │       ├── state_machine.py # StateMachine（状态转换规则）
│   │   │   │       ├── task_manager.py  # TaskManager（任务生命周期 + Trace 事件记录）
│   │   │   │       └── progress.py      # ProgressCalculator（步骤状态→百分比）
│   │   │   │
│   │   │   ├── domain/               # dataclass 版领域模型
│   │   │   │   ├── __init__.py
│   │   │   │   ├── task.py           # Task（聚合根，含内置状态转换规则）
│   │   │   │   ├── workflow.py       # WorkflowDefinition（步骤链遍历）
│   │   │   │   ├── step.py           # StepDefinition（输入校验）
│   │   │   │   └── agent.py          # AgentProfile（能力匹配检查）
│   │   │   │
│   │   │   ├── agents/               # 通用 Agent Interface
│   │   │   │   ├── __init__.py
│   │   │   │   ├── base.py           # BaseAgent（抽象基类）+ AgentProfile + AgentRunContext
│   │   │   │   └── registry.py       # AgentRegistry（按 domain+name 索引，能力标签 fallback）
│   │   │   │
│   │   │   ├── skills/               # 通用 Skill Interface
│   │   │   │   ├── __init__.py
│   │   │   │   ├── base.py           # BaseSkill（抽象基类）+ NoOpSkill
│   │   │   │   └── registry.py       # 占位
│   │   │   │
│   │   │   ├── packs/                # Pack 发现、校验、注册
│   │   │   │   ├── __init__.py
│   │   │   │   └── registry.py       # PackManifest / discover_pack_manifests / register_installed_packs
│   │   │   │
│   │   │   ├── adapters/             # 外部能力 Adapter
│   │   │   │   ├── __init__.py
│   │   │   │   ├── model_adapter.py      # ModelService 协议 / AIService / ModelAdapter
│   │   │   │   ├── retrieval_adapter.py  # 重新导出 retrieval 子模块
│   │   │   │   ├── federated_adapter.py  # FederatedAdapter（联邦学习增强，失败开放）
│   │   │   │   └── retrieval/            # 检索器实现
│   │   │   │       ├── __init__.py
│   │   │   │       ├── chroma_client.py          # ChromaLegalClient（SentenceTransformer 嵌入）
│   │   │   │       ├── legal_index_builder.py     # LegalIndexBuilder（statutes/cases/evidence_rules/limitation/jurisdiction）
│   │   │   │       ├── education_index_builder.py # EducationIndexBuilder（知识点/题库/教案/教学方法）
│   │   │   │       └── code_index_builder.py      # CodeIndexBuilder（多语言 AST 符号索引）
│   │   │   │
│   │   │   ├── stores/               # Workflow Store 实现
│   │   │   │   ├── __init__.py
│   │   │   │   ├── workflow_store.py         # WorkflowStore 抽象基类 + 分页工具
│   │   │   │   ├── memory_workflow_store.py  # MemoryWorkflowStore（字典存储）
│   │   │   │   └── sqlite_workflow_store.py  # SQLiteWorkflowStore（JSON payload）
│   │   │   │
│   │   │   ├── memory/               # 记忆系统（Interface 层）
│   │   │   │   ├── __init__.py
│   │   │   │   ├── base_memory.py    # 占位
│   │   │   │   ├── workflow_memory.py    # WorkflowMemory（步骤间上下文传递）
│   │   │   │   ├── semantic_memory.py    # 占位
│   │   │   │   └── evidence_memory.py    # 占位
│   │   │   │
│   │   │   ├── communication/        # 通信协议（Interface 层—占位）
│   │   │   │   ├── __init__.py
│   │   │   │   ├── protocol.py       # 空（实现见 core/communication/）
│   │   │   │   ├── message.py        # 空
│   │   │   │   └── router.py         # 空
│   │   │   │
│   │   │   ├── governance/           # 治理层（Interface 层—占位）
│   │   │   │   ├── __init__.py
│   │   │   │   ├── policy_engine.py  # 空（实现见 core/governance/）
│   │   │   │   ├── evidence_chain.py # 空
│   │   │   │   ├── trace_logger.py   # 空
│   │   │   │   └── review_manager.py # 空
│   │   │   │
│   │   │   ├── recovery/             # 恢复机制（Interface 层—占位，实现内嵌在 core/）
│   │   │   │   ├── __init__.py
│   │   │   │   ├── checkpoint.py         # 空
│   │   │   │   ├── recovery_manager.py   # 空
│   │   │   │   └── retry_strategy.py     # 空
│   │   │   │
│   │   │   └── infrastructure/       # 基础设施 Adapter 占位
│   │   │       ├── __init__.py
│   │   │       ├── adapters/         # llm_adapter / deepseek_adapter / qwen_adapter 等（全部占位）
│   │   │       ├── stores/           # base_store / task_store / workflow_store（全部占位）
│   │   │       └── repositories/     # memory_repository / sql_repository（全部占位）
│   │   │
│   │   ├── api/                      # FastAPI 入口占位（待实现）
│   │   ├── config/                   # 配置模块占位（待实现）
│   │   └── services/                 # 业务编排占位（待实现）
│   │
│   ├── knowledge/                    # 知识库服务 Interface
│   │   ├── vector_store/             # base_store / chroma_store / pgvector_store
│   │   └── retriever/                # base_retriever / vector_retriever / hybrid_retriever
│   │
│   ├── llm/                          # LLM 网关 Interface
│   │   ├── providers/                # base_provider / openai_provider / deepseek_provider / qwen_provider
│   │   └── router/                   # base_router / cost_router / performance_router
│   │
│   ├── infrastructure/               # 基础设施配置
│   │   └── docker-compose.yml
│   │
│   └── tests/                        # 10 个测试文件
│       ├── test_acg_model.py         # ACG 模型 & 图算法
│       ├── test_acg_promotion.py     # Workflow→ACG 升格
│       ├── test_communication.py     # 低熵通信 & 血缘追踪
│       ├── test_domain_models.py     # 领域模型
│       ├── test_model_adapter.py     # AIService/ModelAdapter
│       ├── test_planning.py          # 认知规划全链路
│       ├── test_progress.py          # 进度计算
│       ├── test_task_manager.py      # 任务生命周期
│       └── test_task_manager_events.py  # 任务事件 & 人审流程
```

```text
agent/                                # 应用服务层
│   ├── app/                          # FastAPI 应用
│   │   ├── main.py                   # FastAPI 入口
│   │   ├── api/                      # 30 个 API 路由文件（chat / agentos_core / rag / voice / federated* …）
│   │   ├── execution/                # LangGraph Adapter 与实现注册
│   │   │   ├── langgraph_adapter.py      # LangGraphAdapter（应用层执行适配器）
│   │   │   ├── langgraph_registry.py     # LangGraphImplementationRegistry
│   │   │   └── runtime.py                # 应用层运行时注册
│   │   ├── graphs/                   # LangGraph StateGraph 实现
│   │   │   └── legal_contract_review_stategraph.py  # 合同审查 7 节点 StateGraph
│   │   ├── llm/                      # LLM Gateway 实现（Provider / Config / Schema）
│   │   ├── rag/                      # RAG 实现（法律文档加载 / 关键词检索 / 文本分块）
│   │   ├── services/                 # 34 个业务服务文件
│   │   ├── middleware/               # 中间件
│   │   └── utils/                    # 工具函数
│   │
│   ├── packs/                        # 领域 Pack（5 个职业）
│   │   ├── legal/                    # 律师 Pack（最完整）
│   │   │   ├── manifest.yaml
│   │   │   ├── agents/               # case_intake / statute / evidence / risk / draft / review
│   │   │   ├── skills/               # 9 个法律技能
│   │   │   ├── workflows/            # 5 个 YAML 工作流（含 ACG 版本和 LangGraph 迁移版本）
│   │   │   └── data/                 # 律师知识库 Markdown
│   │   ├── education/                # 教育 Pack
│   │   │   ├── manifest.yaml
│   │   │   ├── agents/               # lesson_plan
│   │   │   ├── skills/               # 8 个教育技能
│   │   │   ├── workflows/            # lesson_plan workflow
│   │   │   └── data/                 # 教师知识库 Markdown
│   │   ├── programmer/               # 程序员 Pack
│   │   │   ├── manifest.yaml
│   │   │   ├── agents/               # requirement_analysis / code_generation / codebase_search / diagram_generation
│   │   │   ├── skills/               # 4 个编程技能
│   │   │   ├── workflows/            # requirement_analysis workflow
│   │   │   └── data/                 # 技术知识库 Markdown
│   │   └── writer/                   # 作家 Pack
│   │       ├── manifest.yaml
│   │       ├── agents/               # outline_generate
│   │       ├── skills/               # 4 个写作技能
│   │       ├── workflows/            # story_outline workflow
│   │       └── data/                 # 文学知识库 Markdown
│   │
│   ├── tests/                        # 20+ 测试文件（覆盖 ACG / 联邦学习 / 合同审查 / Pack 注册等）
│   ├── requirements.txt
│   ├── requirements_minimal.txt
│   └── Dockerfile
```

```text
agentOS/knowledge/                        # 知识库服务 Interface（实际实现在 agent/app/rag/）
│   ├── vector_store/                     # 向量存储
│   │   ├── base_store.py
│   │   ├── chroma_store.py
│   │   └── pgvector_store.py
│   └── retriever/                        # 检索器
│       ├── base_retriever.py
│       ├── vector_retriever.py
│       └── hybrid_retriever.py
```

```text
agentOS/llm/                              # LLM 网关 Interface（实际实现在 agent/app/llm/）
│   ├── providers/                        # 模型提供商适配
│   │   ├── base_provider.py
│   │   ├── openai_provider.py
│   │   ├── deepseek_provider.py
│   │   └── qwen_provider.py
│   └── router/                           # 路由策略
│       ├── base_router.py
│       ├── cost_router.py
│       └── performance_router.py
```

> **注**：`shared/` 目录未独立建立，共享类型和工具分散在各模块中。
>
> AGI `infrastructure/` 仅包含 `docker-compose.yml` 占位文件，实际部署编排在项目根目录 `docker-compose.yml`。

---

## 架构演进：设计 vs 实际实现

原始架构设计偏向扁平 Interface 定义层，实际实现演进为 **"Interface 层(顶层) + Implementation 层(core/子包)"** 的双层结构。

### 双层结构模式

| 模块 | 顶层（Interface） | core/ 子包（Implementation） |
|---|---|---|
| **governance** | `agentos/governance/` 4 个空占位文件 | `core/governance/` TraceStore / CheckpointStore / ReviewManager / WorkflowEvaluator |
| **communication** | `agentos/communication/` 3 个空占位文件 | `core/communication/` ContextAssembler / ContextPack / ProvenanceLedger |
| **models** | `agentos/domain/` dataclass 版本 | `core/models/types.py` Pydantic v2 版本（核心运行时使用） |
| **recovery** | `agentos/recovery/` 3 个空占位文件 | 恢复逻辑内嵌在 `core/governance/checkpoint.py` 和 `core/runtime.py` |

### 设计文档未覆盖的重大子系统

以下子系统在实际实现中存在，但原始架构设计未描述：

#### 1. ACG 动态群体智能引擎 (`core/acg/`)

统一计算图模型，是整个动态异构群体智能架构的核心数据结构和执行载体。

- **7 种节点类型**：StepNode / AgentNode / SkillNode / MemoryNode / EvidenceNode / ControlNode (START/END/IF/LOOP/PARALLEL/CONSENSUS)
- **统一边模型**：ACGEdge 支持 DEPENDENCY / COMMUNICATION / CONTROL_FLOW / EXECUTION / WRITE / READ / SUPPORT
- **图算法**：环检测(Kahn)、拓扑排序、悬空依赖检测、就绪集计算（并行调度核心）
- **升格机制**：`promote_workflow_to_acg()` 将线性 WorkflowDefinition 升格为带认知节点的 ACGBlueprint

#### 2. 执行适配器层 (`core/execution/`)

- **ExecutionAdapter 协议**：`start(task, run, workflow)` + `apply_review(decision)`
- **ACGExecutor**：基于就绪集的并行步骤调度引擎，集成低熵通信（ContextAssembler），支持人审中断和故障注入/自愈
- **FaultInjector**：可控故障注入（TIMEOUT / CRASH / EMPTY_EVIDENCE），用于验证自愈能力

#### 3. 认知规划引擎 (`core/planning/`)

实现"静态优选，动态补位"策略：

```
IntentParser（意图解析）
  ├── LLM 结构化 JSON 解析路径
  └── 关键词确定性启发式 fallback
       ↓
TemplateMatcher（模板匹配）
  └── domain 一级索引 + intent 二级索引 + n-gram Dice 相似度
       ├── 命中 → promote_workflow_to_acg() → ACGBlueprint
       └── 未命中 ↓
CognitiveRouter（认知路由）
  └── 多维效用评分 + 临时角色生成
       ↓
ACGBuilder（动态 ACG 构造）
  └── 语义画像 → 物化 ACGBlueprint（含 PARALLEL/CONSENSUS 控制节点）
```

### 文件统计

| 类别 | 数量 |
|---|---|
| 核心实现文件（有实质代码） | ~33 个 |
| `__init__.py` 导出模块 | ~34 个 |
| 空占位文件 | ~67 个 |
| **总计 .py 文件** | ~134 个 |
| 代码量最大文件 | `core/runtime.py`（~668 行） |

### 顶层 agentOS/ 与 core/ 职责划分

- **顶层 `agentos/` 目录**：存放 Abstract Interface / Base Class / Protocol / Registry 等可被外部直接引用的抽象定义
- **`core/` 子目录**：存放完整的运行时实现，是实际执行逻辑所在位置
- **`infrastructure/`、`recovery/`、`communication/`（顶层）、`governance/`（顶层）**：当前为预留占位，未来作为 Interface 注册入口，实现逐步向 core/ 收敛

