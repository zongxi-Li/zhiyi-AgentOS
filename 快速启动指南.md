# Kinlin AI 系统快速启动指南

## 当前状态

✅ **已启动的服务：**
- Python AI服务（端口8000）- 后台运行中
- Spring Boot后端（端口8080）- 后台运行中  
- Vue前端（端口5173）- 后台运行中

⚠️ **需要启动的服务：**
- PostgreSQL数据库（端口5432）- 需要Docker Desktop
- Redis缓存（端口6379）- 需要Docker Desktop

## 启动数据库和Redis

### 方法1：使用Docker Desktop（推荐）

1. **启动Docker Desktop**
   - 打开Docker Desktop应用程序
   - 等待Docker完全启动（状态显示为"Running"）

2. **启动数据库和Redis**
   ```bash
   cd docker
   docker-compose up -d postgres redis
   ```

3. **验证服务状态**
   ```bash
   docker ps
   ```
   应该看到 `kinlin-postgres` 和 `kinlin-redis` 容器在运行

### 方法2：使用启动脚本

**Windows批处理脚本：**
```bash
start.bat
```

**PowerShell脚本：**
```powershell
.\start.ps1
```

## 服务地址

启动成功后，可以通过以下地址访问：

- **前端界面**: http://localhost:5173
- **后端API**: http://localhost:8080
- **AI服务**: http://localhost:8000
- **API文档**: http://localhost:8080/swagger-ui.html (如果配置了Swagger)

## 验证服务状态

### 检查端口监听
```bash
netstat -an | findstr "8000 8080 5173 5432 6379"
```

### 检查服务健康状态

**AI服务：**
```bash
curl http://localhost:8000/health
```

**后端服务：**
```bash
curl http://localhost:8080/actuator/health
```

## 停止服务

### 停止Docker服务
```bash
cd docker
docker-compose down
```

### 停止其他服务
- 在任务管理器中结束对应的进程
- 或者关闭对应的命令行窗口

## 常见问题

### 1. Docker Desktop未运行
**错误信息：** `unable to get image` 或 `The system cannot find the file specified`

**解决方法：**
- 启动Docker Desktop应用程序
- 等待Docker完全启动后再运行docker-compose命令

### 2. 端口被占用
**错误信息：** `Address already in use` 或 `端口已被使用`

**解决方法：**
- 检查是否有其他程序占用端口
- 修改配置文件中的端口号
- 停止占用端口的程序

### 3. 数据库连接失败
**错误信息：** `Connection refused` 或 `无法连接到数据库`

**解决方法：**
- 确认PostgreSQL容器正在运行：`docker ps`
- 检查数据库配置：`backend/src/main/resources/application.yml`
- 确认数据库用户名和密码正确

### 4. 前端依赖未安装
**错误信息：** `Cannot find module` 或 `npm ERR!`

**解决方法：**
```bash
cd frontend
npm install
```

### 5. Maven构建失败
**错误信息：** `BUILD FAILURE` 或依赖下载失败

**解决方法：**
```bash
cd backend
mvn clean install
```

## 开发模式

### 热重载
- **Python AI服务**: 已启用 `--reload` 参数，代码修改后自动重启
- **Vue前端**: Vite默认支持热重载
- **Spring Boot后端**: 需要配置DevTools或手动重启

### 日志查看
- **Python AI服务**: 查看运行窗口的输出
- **Spring Boot后端**: 查看运行窗口的输出，或查看 `backend/logs/` 目录
- **Vue前端**: 查看浏览器控制台和运行窗口的输出

## 下一步

1. 启动Docker Desktop
2. 运行 `docker-compose up -d postgres redis` 启动数据库
3. 访问 http://localhost:5173 使用系统
4. 如果遇到问题，查看各服务的日志输出

---

**提示：** 所有服务都在后台运行，可以通过任务管理器查看进程状态。


